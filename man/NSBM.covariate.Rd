% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NSBM_covariate.R
\name{NSBM.covariate}
\alias{NSBM.covariate}
\title{Covariate-based species distribution model....}
\usage{
NSBM.covariate(
  nsbm_obj,
  family = binomial(link = "logit"),
  spde.pcprior.range = c(5, 0.01),
  spde.pcprior.sigma = c(1, 0.01),
  spde.mesh = NULL,
  rm.corr = TRUE,
  corcut = 0.7,
  proj.new.env = TRUE,
  n.threads = 1,
  cv.folds = 1,
  seed = NULL,
  save.output = FALSE,
  save.independent = FALSE
)
}
\arguments{
\item{nsbm_obj}{An object of class \code{nsdm.vinput}, result from \code{sabinaNSDM::NSDM.SelectCovariates()}.}

\item{family}{A standard R \code{family} object (e.g. \code{binomial(link="logit")}), or the character string \code{"cp"} to fit a Cox point process (intensity).}

\item{spde.pcprior.range}{Numeric vector length 2. Prior on spatial range (e.g., \code{c(5, 0.01)}).}

\item{spde.pcprior.sigma}{Numeric vector length 2. Prior on marginal standard deviation (e.g., \code{c(1, 0.01)}).}

\item{spde.mesh}{An INLA mesh object created externally with \code{create_mesh()}. includes spatial latent field (SPDE) in the model (default: NULL).}

\item{rm.corr}{(\emph{optional, default} \code{TRUE}) \cr
A \code{logical} controlling whether environmental covariates correlated with the global model should be removed. The threshold value used for identifying collinearity is the same used with \code{\link{NSDM.SelectCovariates}} function.
#@param corcut (\emph{optional, default} \code{0.7}) \cr
#A \code{numeric} value for the correlation coefficient threshold used for identifying collinearity.   #@@JMB ponerlo como argumento en lugar de traerlo de NSDM.SelectCovariates()???}

\item{proj.new.env}{Logical; whether to compute predictions under new scenarios (default: TRUE).}

\item{n.threads}{Number of threads for analysis}

\item{cv.folds}{Number of k-folds for cross-validation (default: 1 = no CV). If >1, returns mean ± sd AUC.}

\item{seed}{Optional integer to set the random seed for reproducibility.}

\item{save.output}{Logical; if TRUE, saves key model outputs (predictions, evaluation, summary ...).}

\item{save.independent}{Logical; if \code{TRUE}, also saves the individual global and regional model vlaues and predictions.}
}
\value{
A named list of class \verb{nsbm.inlabru, including: \\item\{Species.Name\}\{Species name.\} \\item\{args\}\{List of arguments used in the function.\} \\item\{Selected.Variables.Global\}\{Names of selected global-scale covariates.\} \\item\{Selected.Variables.Regional\}\{Names of selected regional-scale covariates.\} \\item\{Selected.Variables.Covariate\}\{Names of regional covariates after dropping high correlation.\} \\item\{current.projections\}\{List with prediction: \code{pred.covariate} and, if requested, separate \code{pred.global}.\} \\item\{new.projections\}\{List of combined projections under new environmental scenarios (if }proj.new.env = TRUE\verb{).\} \\item\{Summary\}\{A }data.frame` with key evaluation metrics and significant variables.}
}
\description{
bla bla...
}
\details{
\itemize{
\item binomial(logit): Use for presence–absence (1/0) data. Output: probability.    #@@JMB revisar todo lo relacionado con family-link (opciones, inverse link in pred, ...)
\item binomial(cloglog): Use for presence–absence (1/0) data when 1s are very rare (lots of 0s). Output: probability.
\item poisson(log): Use for non-overdispersed counts (e.g. abundance counts 0,1,2…). Output: expected count
\item nbinomial(log): Use for overdispersed counts (variance > mean) (e.g. xx). Output: expected count
\item gaussian(identity): Use for continuous responses normal distribution (e.g. xx). Output: predicted value on original scale.
\item gaussian(log): Use for strictly positive, skewed continuous data (e.g. xx). Output: predicted value on original scale.
\item beta(logit): Use for proportions in (0,1) (e.g. rescaled canopy cover). Output: predicted proportion
\item tweedie(log): Use for semicontinuous data with many zeros and a continuous positive tail (e.g. xx). Output: expected value
\item cp: (Cox process) Use for presence-only data or spatial point patterns. Output: intensity.
}
}
