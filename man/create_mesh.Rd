% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_mesh.R
\name{create_mesh}
\alias{create_mesh}
\title{Create INLA mesh for spatial modeling}
\usage{
create_mesh(
  nsdm_obj,
  edge = c(0.5, 1),
  offset = c(0.25, 0.5),
  buffer = 0.01,
  plot = FALSE
)
}
\arguments{
\item{nsdm_obj}{An object of class \code{nsdm.vinput} as returned by \code{sabinaNSDM::NSDM.SelectCovariates()}.}

\item{edge}{A numeric vector of length 2. Maximum triangle edge lengths for the inner and outer parts of the mesh (default: \code{c(0.5, 1)}). #@@JMB los valores por defecto son para crs en grados, habrá que ajustar cosas aquí para cuando los rasters sean metros}

\item{offset}{A numeric vector of length 2. Offsets to expand the domain inward and outward from the boundary (default: \code{c(0.25, 0.5)}).}

\item{buffer}{A numeric scalar. Amount to buffer (expand) the convex hull before mesh creation (default: \code{0.01}).}

\item{plot}{Logical. If TRUE, plots the mesh with context (presence points and subtitle). Default is FALSE.}
}
\value{
An INLA mesh object (\code{inla.mesh}). If \code{plot = TRUE}, the plot is also printed for inspection.
}
\description{
Generates a 2D mesh object using the convex hull of the global species presences
from a processed \code{nsdm.vinput} object (output of \code{\link{NSDM.SelectCovariates}}).
}
