% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_mesh.R
\name{create_mesh}
\alias{create_mesh}
\title{Create INLA mesh for spatial modeling}
\usage{
create_mesh(
  nsdm_obj,
  edge = c(0.5, 1),
  offset = c(0.25, 0.5),
  buffer = 0.01,
  boundary.method = "convex_hull",
  concavity = NULL,
  remove_holes = FALSE,
  proj.new.env = TRUE,
  plot = FALSE
)
}
\arguments{
\item{nsdm_obj}{An object of class \code{nsdm.vinput} as returned by \code{sabinaNSDM::NSDM.SelectCovariates()}.}

\item{edge}{A numeric vector of length 2. Maximum triangle edge lengths for the inner and outer mesh domains (default: \code{c(0.5, 1)}). Units must match the CRS of the input rasters (degrees for geographic CRS, metres for projected CRS). Note: default values (\code{c(0.5, 1)}) assume a geographic CRS in degrees. For projected CRS in metres, scale accordingly (e.g., \code{c(50000, 100000)} for UTM in metres).}

\item{offset}{A numeric vector of length 2. Offsets to expand the domain inward and outward from the boundary (default: c(0.25, 0.5)).}

\item{buffer}{A numeric scalar. Amount to buffer (expand) the convex hull before mesh creation (default: 0.01).}

\item{boundary.method}{Character. One of \code{"convex_hull"} (default), \code{"concave_hull"}, or \code{"raster_mask"}. \code{"raster_mask"} constrains the mesh to the raster's non-NA cells, avoiding triangulation over gaps (e.g., fragmented distributions, coastal/oceanic areas); \code{"convex_hull"}/\code{"concave_hull"} triangulate the full hull regardless of gaps.}

\item{concavity}{Numeric. Required when \code{boundary.method = "concave_hull"}. Controls how tightly the concave hull wraps the points; lower values (e.g., 2–3) produce tighter boundaries.}

\item{remove_holes}{Logical. Only used when \code{boundary.method = "raster_mask"}; if TRUE, internal holes are removed before mesh creation (default: FALSE).}

\item{proj.new.env}{Logical. If TRUE, includes the extent of new scenarios in the mesh domain if they exist in \code{nsdm_obj$Scenarios} (default: TRUE). Only applies if \code{boundary.method = "raster_mask"}.}

\item{plot}{Logical. If TRUE, plots the mesh for inspection (default: FALSE).}
}
\value{
An \code{fm_mesh_2d} object (class \code{jmbm.mesh}) suitable for use with \code{INLA} and \code{inlabru} SPDE components. Pass directly to \code{MBM.Modelling(spde.mesh = ...)}.
}
\description{
Generates a 2D mesh object using the convex hull, concave_hull, or raster_mask of the species presences/absences (plus optional new scenario rasters), from a processed \code{nsdm.vinput} object (output of \code{\link[sabinaNSDM]{NSDM.SelectCovariates}}).
}
\references{
Lindgren, F., Rue, H. & Lindström, J. (2011). An explicit link between Gaussian fields
and Gaussian Markov random fields: the stochastic partial differential equation approach.
\emph{Journal of the Royal Statistical Society B}, 73, 423–498.
}
\seealso{
\code{\link{MBM.Modelling}}
}
